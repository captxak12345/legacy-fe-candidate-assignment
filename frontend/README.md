### 🧩 Frontend (React)

This is a minimal frontend app built with Vite and React.

## Prerequisites
- Node.js (v16 or higher recommended)
- npm (comes with Node.js)

## Setup
1. **Install dependencies**
   ```sh
   npm install
   ```

2. **Run the development server**
   ```sh
   npm run dev
   ```
   The app will be available at the URL shown in the terminal (usually http://localhost:5173).

## Project Structure
- `src/` - Source code
- `src/App.tsx` - Main React component
- `src/pages/` - App pages
- `src/components/` - Reusable components

## Common Commands
- `npm install` - Install dependencies
- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run preview` - Preview production build

---

For any issues, check the code comments or contact the maintainer.
